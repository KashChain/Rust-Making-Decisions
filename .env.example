# Optional override. Production defaults to the first-party /api/responses endpoint.
VITE_FORM_ENDPOINT=
# Optional JSON endpoint for anonymous product events. No form values are included.
VITE_ANALYTICS_ENDPOINT=
